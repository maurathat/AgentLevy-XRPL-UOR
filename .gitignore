# --- Secrets & local config ---
.env
.env.local
.env.*.local

# Local-only pitch documents (not for public source control)
pitch/kessai-funding-deck.md
pitch/kessai-onepager.md

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
build/
dist/
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# Test / type-check / lint caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/

# Jupyter
.ipynb_checkpoints/

# --- LLM cache: ad-hoc captures only ---
# fixtures/ IS checked in (deterministic demo cache).
# fixtures/live/ is for throwaway captures during dev.
fixtures/live/

# --- Editors / IDEs ---
.vscode/
.idea/
*.swp
*.swo
*~

# --- macOS ---
.DS_Store
.AppleDouble
.LSOverride
Icon?

# --- Logs & temp ---
*.log
*.tmp
tmp/

# --- PRISM clone (if cloned into the repo root, installed via pip -e) ---
# Anchored to repo root so it does NOT match agentlevy/prism/ (our wrapper package)
/prism/
